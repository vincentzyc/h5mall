$themm-color = #fc4d56;
$orange-color = #fb8b44;

.banner-slide {
  width: 100%;
  margin-top: 44px;

  .banner-slide-item {
    height: 200px;

    img {
      width: 100%;
    }
  }

  .cube-slide-dots {
    text-align: right;
    padding-right: 20px;
    padding-bottom: 10px;

    span {
      height: 10px;
      border-radius: 100%;
      border: 1px solid $themm-color;
      background: transparent;

      &.active {
        background: $themm-color;
      }
    }
  }
}

.header-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 44px;
  line-height: 44px;
  text-align: center;
  -webkit-backface-visibility: hidden;
  backface-visibility: hidden;
  z-index: 5;
  background: -webkit-linear-gradient(left, $orange-color, $themm-color); /* Safari 5.1 - 6.0 */
  background: -o-linear-gradient(right, $orange-color, $themm-color); /* Opera 11.1 - 12.0 */
  background: -moz-linear-gradient(right, $orange-color, $themm-color); /* Firefox 3.6 - 15 */
  background: linear-gradient(to right, $orange-color, $themm-color); /* 标准的语法 */
}

.footer-wrap {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 44px;
  line-height: 44px;
  border-top: 1px solid #eee;
  background: #fff;
  z-index: 5;
}